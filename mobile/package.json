{
  "name": "ggallery-mobile",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "sync": "npx cap sync",
    "copy": "npx cap copy",
    "open": "npx cap open android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
